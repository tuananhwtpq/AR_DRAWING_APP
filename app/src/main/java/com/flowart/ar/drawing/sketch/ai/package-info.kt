package com.flowart.ar.drawing.sketch.ai

/**
 * Package chứa các module AI/ML của FlowARt.
 *
 * Các class trong package này:
 * - BackgroundRemover: Xóa nền ảnh bằng ML Kit Subject Segmentation
 * - ImageAnalyzer: Nhận diện nội dung ảnh bằng ML Kit Image Labeling
 * - CategoryMapper: Mapping label AI → category trong database
 * - DrawingEvaluator: Đánh giá bản vẽ bằng OpenCV metrics (SSIM, Histogram, Contour)
 * - HeatmapGenerator: Tạo heatmap so sánh bản vẽ vs template
 */
